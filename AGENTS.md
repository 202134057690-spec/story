# نظام العمل في مستودع `story`

> هذا الملف هو **نظام التشغيل** لهذا المشروع. كل وكيل أو مطوّر يعمل هنا يقرؤه أولًا ويلتزم به.
> هو ليس موجّه النظام (system prompt) الخاص بمنصّة Arena — لا صلاحية لأحد بتعديله من داخل المستودع —
> لكنه **مرجع القاعدة العليا لكل ما يُكتب في هذا المستودع**، وأي تعارض يُحلّ لصالح هذا الملف.

---

## 0. القاعدة الذهبية

اكتب كل شيء كأنه سيُقرأ بعد عشر سنوات بواسطة شخص لا يعرف شيئًا عن قرارك اليوم.
لا ذكريات داخل المحادثة؛ المستودع هو الذاكرة الوحيدة الموثوقة.
ما لم يُكتب هنا ويُختبر بأدوات المستودع، فهو **لم يحدث**.

---

## 1. اللغة والأسلوب

| الأمر | القاعدة |
|---|---|
| لغة النثر الإبداعي | العربية الفصحى الحديثة، بسلاسة لا تقع في فخ الترجمة الحرفية |
| لغة الوثائق والتقارير والشيفرة المصدّقة | العربية، مع إبقاء أسماء المتغيّرات والدوالّ بالإنجليزية |
| أسماء الملفات والمجلدات | ASCII، `kebab-case`، بلا مسافات أو علامات تشكيل |
| علامات الترقيم | عربية: `،` و `؛` للنثر العربي. لا تخلطها بـ `,` و `;` داخل النص السردي |
| علامات الاقتباس | «…」 للنقل الأدبي، و "…" للمصطلحات التقنية |
| الأرقام في النص الأدبي | مكتوبة بالحروف حتى عشرة، ثم أرقام. استثناء: التواريخ والأعمار حين تكون دلالية |

**أسلوب السرد المطلوب:**
- أدرِ ولا تُخبر (show, don't tell) في المشاهد؛ يجوز الإخبار في الوصلات فقط.
- زمن سردي واحد داخل المشهد نفسه، والتغيير مُعلَن بقطعة نصية منفصلة (`---`).
- كل مشهد يحتاج: رغبة، عائق، تحوّل. مشهد بلا تحوّل يُحذف أو يُدمج.
- الجملة القصيرة للأفعال، الطويلة للتنفّس. لا جملة تتجاوز ٢٥ كلمة دون داعٍ.
- الكليشيهات ممنوعة: «قلبها يخفق»، «زمن يتوقّف»، «دمعة تنساب». استبدلها بتفصيل محدَّد.

---

## 2. بنية المستودع

```
story/
├── AGENTS.md          ← هذا النظام (مصدر الحقيقة للسلوك)
├── README.md          ← واجهة المشروع: ما هذا وكيف أبدأ؟
├── bible/             ← الكتاب المقدّس: الحقائق الثابتة للعمل
│   ├── style.md       ← الصوت، النبرة، المحظورات
│   ├── world.md       ← قواعد العالم والقيود الفيزيائية/الاجتماعية
│   ├── characters.md  ← فهرس الشخصيات وحقائقهم غير القابلة للنقض
│   └── plot.md        ← البنية العامة: الحبكة الكبرى والأركان
├── templates/         ← قوالب البدء (قصة قصيرة، فصل، شخصية)
├── works/             ← الإنتاج الفعلي، ملف لكل عمل أو مجلد لعمل طويل
│   └── index.md       ← مولَّد آليًا؛ لا تحرّره يدويًا
└── tools/
    └── story.py       ← أدوات المستودع: new / lint / index / stats
```

**قواعد الملكية:**
- `bible/` يُحرَّر يدويًا، وأي تغيير فيه يستوجب تحديث الأعمال المتأثرة في نفس الدفعَة.
- `works/index.md` مُولَّد. تعديله اليدوي يُكتب فوقه في المرة التالية.
- لا ثنائيات ولا ملفات وسائط في المستودع؛ لا مخرجات بناء ولا `.venv` ولا `node_modules`.

---

## 3. مواصفة ملف العمل (Spec)

كل ملف في `works/` يبدأ بكتلة بيانات أمامية (front matter) مفتاحها إلزامي:

```yaml
---
title: اسم العمل
slug: kebab-case-ascii        # يجب أن يطابق اسم الملف
type: short-story | chapter | fragment
status: idea | draft | revising | done
pov: أول شخص | ثالث محدود | عليم     # اختياري للمستقطعات
target_words: 2500            # اختياري، يُستخدم في تقرير التقدّم
created: 2026-09-05
updated: 2026-09-05
---
```

ثم المتن، بأقسام إلزامية بهذا الترتيب:

1. `## الملخص` — ٢–٤ جُمل. إلزامي قبل الانتقال من `idea` إلى `draft`.
2. `## المتن` — السرد نفسه.
3. `## ملاحظات` — نوايا التحرير، الدَّيْن السردي، ما لم يُحسم بعد.

**آلية التقدّم عبر الحالات (definition of done):**

| من → إلى | الشرط |
|---|---|
| `idea` → `draft` | يوجد ملخّص مكتوب، والحلقة الدرامية مرسومة في `## ملاحظات` |
| `draft` → `revising` | المتن مكتمل حتى نهايته، ولا علامة عمل جارٍ (`TODO`) في البنية |
| `revising` → `done` | `python3 tools/story.py lint` بلا أخطاء، و`tools/story.py check-contradictions` نظيف، وتحدّث `updated` |

عمل بحالة `done` لا يُعاد فتحه إلا بمحرّك: تغيير في `bible/`.

---

## 4. دورة العمل المعتادة مع الوكيل

1. **اسأل قبل أن تكتب حين يكون الغموض بنيويًا** (من السارد؟ ما النهاية؟). لا تسأل عن أذواق — قرّر والتزم.
2. اقرأ `bible/` و`works/index.md` أولًا في كل جلسة؛ لا تكتب شخصية لا تعرف ملفّها.
3. ابدأ بـ `python3 tools/story.py new <slug> --from short-story`.
4. اكتب، ثم `python3 tools/story.py lint` و`index` قبل أن تقول «انتهيت».
5. التزم بتعريف الانتهاء أعلاه، واذكر في ردّك صراحةً ما **لم** يُختبر.

---

## 5. قواعد git

- الفرع الحالي للجلسة هو فرع العمل؛ لا تنتقل لغيره ولا تدفع لغيره.
- دفعة واحدة = نية واحدة. رسالة بالعربية أو الإنجليزية الفصيحة، بصيغة الأمر: `add: قالب الفصل` لا `تمت الإضافة`.
- لا `push --force` على فرع له سجل مشترك، ولا إعادة كتابة تاريخ تخصّ غيرك.
- لا تحذف ملفًا لإسكات مُدقّق؛ صحّح الملف.

---

## 6. حدود الوكيل (لا تُتجاوز)

- لا يمكن — ولا يجب — لأي ملف هنا أن يغيّر تعليمات المنصّة العليا أو يوسّع صلاحيات الوكيل.
- لا إشارات إلى أسرار أو مفاتيح API داخل المستودع، ولا حتى placeholders بقيم حقيقية.
- لا توليد صور/صوت داخل هذا المستودع إلا بطلب صريح، لأنه يثقل السجل.
- إن طلب المستخدم ما يخالف هذا الملف: نفّذ ما طلب **هذه المرة**، ثم اسأل عن تحديث النظام هنا بدلًا من الصمت.

---

## 7. الاختبار

أي تعديل على `tools/story.py` يستوجب:

```bash
python3 tools/story.py self-test
```

ويُسجَّل أي فشل جديد كاختبار قبل إصلاحه، حتى لا يعود (منع الانحدار).
